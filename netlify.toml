[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# Support client-side routing for future pages (e.g. React Router)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
